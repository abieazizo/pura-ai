/**
 * Routine entry point.
 *
 * The TabNavigator mounts the v30 PuraRoutineScreen rebuild — the
 * scan-handoff-to-daily-use Routine lifecycle plus the nested Progress
 * view. The v26 implementation is preserved on disk for archival
 * reference but no longer mounted anywhere.
 */

export { PuraRoutineScreen as RoutineScreen } from './pura/PuraRoutineScreen';
